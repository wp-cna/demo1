name = "wpcna2-ask-white-plains"
main = "src/index.js"
compatibility_date = "2026-04-21"

# Secrets (set with `wrangler secret put`):
#   OPENAI_API_KEY

[vars]
AI_PROVIDER = "openai"
OPENAI_MODEL = "gpt-5-mini"
ALLOWED_ORIGINS = "https://never-nude.github.io,http://localhost:8080,http://127.0.0.1:8080"
MAX_SOURCES = "6"
